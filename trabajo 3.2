# Solicitar al usuario que ingrese tres números
numero1 = float(input("Ingrese el primer número: "))
numero2 = float(input("Ingrese el segundo número: "))
numero3 = float(input("Ingrese el tercer número: "))

# Ordenar los números de forma descendente utilizando una lista y el método sort()
numeros_descendentes = [numero1, numero2, numero3]
numeros_descendentes.sort(reverse=True)

# Mostrar los números ordenados de forma descendente
print("Los números ordenados de forma descendente son:", numeros_descendentes)
