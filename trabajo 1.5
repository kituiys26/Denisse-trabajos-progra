# Inicializar una variable para almacenar la suma de las edades
suma_edades = 0

# Solicitar al usuario que ingrese la edad de cada persona y sumar las edades
for i in range(5):
    edad = int(input("Ingrese la edad de la persona {}: ".format(i + 1)))
    suma_edades += edad

# Calcular el promedio de las edades
promedio = suma_edades / 5

# Mostrar el resultado
print("La edad promedio de las personas ingresadas es:", promedio)
