# Solicitar al usuario que ingrese el número y la potencia
base = float(input("Ingrese el número base: "))
potencia = int(input("Ingrese la potencia a la que desea elevar el número: "))

# Calcular el resultado de elevar el número a la potencia
resultado = base ** potencia

# Mostrar el resultado
print("El resultado de elevar", base, "a la potencia", potencia, "es:", resultado)
